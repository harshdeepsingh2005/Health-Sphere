# Root-level requirements.txt for Vercel Python runtime
# This mirrors healthsphere/requirements.txt
# Vercel looks for requirements.txt at the root (or same dir as the build output)

Django>=4.2,<5.0
gunicorn>=21.2.0
whitenoise>=6.6.0
django-crispy-forms>=2.1
crispy-bootstrap5>=2024.2
pyotp>=2.9.0
qrcode>=7.4.2
Pillow>=10.2.0
pytz>=2024.1
python-dotenv>=1.0.0
google-generativeai>=0.5.0
